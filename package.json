{
  "name": "@test-mono/root",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --parallel",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap --hoist"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  },
  "devDependencies": {
  }
}
